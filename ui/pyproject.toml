[tool.poetry]
name = "watchdog-ui"
version = "0.5.9"
description = "Streamlit dashboard for Watchdog HTTP monitoring"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.31.0"
httpx = "^0.26.0"
pandas = "^2.2.0"
plotly = "^5.18.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
