services:
  database:
    image: timescale/timescaledb:latest-pg14
    container_name: watchdog_db
    restart: always
    environment:
      POSTGRES_USER: ${DB__USER}
      POSTGRES_PASSWORD: ${DB__PASS}
      POSTGRES_DB: ${DB__NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB__USER} -d ${DB__NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: watchdog_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: ./src
    container_name: watchdog_app
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    restart: always
    expose:
      - "8000"
    env_file:
      - .env
    environment:
      DB__HOST: database
      REDIS__R_HOST: redis
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
      - /app/src/__pycache__
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  worker:
    build: ./src
    container_name: watchdog_worker
    # --watch src for development
    command: arq src.worker.main.WorkerSettings --watch src
    restart: always
    env_file:
      - .env
    environment:
      DB__HOST: database
      REDIS__R_HOST: redis
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./src:/app/src  
      - ./logs:/app/logs

  telegram-bot:
    build: ./src
    container_name: watchdog_telegram
    command: python -m src.telegram.bot
    restart: always
    env_file:
      - .env
    environment:
      DB__HOST: database
      REDIS__R_HOST: redis
    depends_on:
      database:
        condition: service_healthy
    volumes:
      - ./src:/app/src

  ui:
    build: ./ui
    container_name: watchdog_ui
    restart: always
    expose:
      - "8501"
    depends_on:
      - app
    deploy:
      resources:
        limits:
          memory: 512M

  nginx:
    build: ./nginx
    container_name: watchdog_nginx
    restart: always
    ports:
      - "80:80"
    depends_on:
      - app
      - ui

volumes:
  postgres_data:
  redis_data: